var Wind = require("../models/wind");
var assert = require("assert");

describe("Wind", function(){
  var wind;

  before(function(){
    user = new Wind("");
  })

  it ("", function(){
    assert.strictEqual(, );
  });
});
